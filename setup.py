from os import name
from setuptools import find_packages, setup, version

setup(
    name="",
    packages=find_packages(include=["..."]),
    version="0.1.0",
    description="",
    author="",
    license="MIT",
    install_requires=[],
    setup_requires=[],
    tests_require=[],
    test_suite="",
)
